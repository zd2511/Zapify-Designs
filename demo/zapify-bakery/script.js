const items=[];document.querySelectorAll('[data-add]').forEach(b=>b.addEventListener('click',()=>{items.push(b.dataset.add);document.querySelector('#count').textContent=items.length;document.querySelector('#items').value=items.join(', ');b.textContent='Added ✓';setTimeout(()=>b.textContent='Add to order',900)}));document.querySelector('#orderForm')?.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(e.currentTarget);if(!items.length){alert('Please add a cake first.');return}const t=`Hi Zapify Designs! Zapify Bakery demo order.%0AName: ${encodeURIComponent(d.get('name'))}%0APhone: ${encodeURIComponent(d.get('phone'))}%0APayment: ${encodeURIComponent(d.get('payment'))}%0AItems: ${encodeURIComponent(items.join(', '))}`;window.open(`https://wa.me/27743899657?text=${t}`,'_blank')});