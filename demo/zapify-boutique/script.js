const cart=[];const $=s=>document.querySelector(s);const $$=s=>[...document.querySelectorAll(s)];
function render(){const box=$('#cartItems');box.innerHTML=cart.length?cart.map((x,i)=>`<div class="cart-row"><div><h4>${x.name}</h4><p>Qty 1 · R${x.price}</p></div><button class="remove" data-i="${i}">Remove</button></div>`).join(''):'<p>Your bag is empty.</p>';const total=cart.reduce((a,x)=>a+x.price,0);$('#cartTotal').textContent='R'+total.toLocaleString();$('#payAmount').textContent='R'+total.toLocaleString();$('#cartCount').textContent=cart.length}
function openCart(){ $('#drawer').classList.add('open');$('#backdrop').classList.add('show') }function closeCart(){ $('#drawer').classList.remove('open');$('#backdrop').classList.remove('show') }
$$('.add').forEach(btn=>btn.onclick=()=>{const p=btn.closest('.product');cart.push({name:p.dataset.name,price:+p.dataset.price});render();openCart()});document.addEventListener('click',e=>{if(e.target.matches('.remove')){cart.splice(+e.target.dataset.i,1);render()}});$('#cartOpen').onclick=openCart;$('#cartClose').onclick=closeCart;$('#backdrop').onclick=closeCart;$('#hamb').onclick=()=>$('#nav').classList.toggle('open');
function showPay(){closeCart();$('#modal').classList.add('show')}$('#checkoutBtn').onclick=showPay;$('#checkoutBtn2').onclick=()=>cart.length?showPay():alert('Add a product to your bag first.');$('#modalClose').onclick=()=>$('#modal').classList.remove('show');$('#payForm').onsubmit=e=>{e.preventDefault();alert('Demo payout complete. No real payment was processed.');$('#modal').classList.remove('show')};render();
$$('.filter').forEach(b=>b.onclick=()=>{$$('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');const f=b.dataset.filter;$$('.product').forEach(p=>p.style.display=f==='all'||p.dataset.cat===f?'':'none')});
