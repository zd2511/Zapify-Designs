let start=0;addEventListener('scroll',()=>{const hero=document.querySelector('.hero-car');if(!hero)return;const y=Math.min(1,Math.max(0,scrollY/650));hero.style.transform=`scale(${1+y*.08}) translateX(${y*-4}%)`;hero.style.filter=`saturate(${.85+y*.65}) hue-rotate(${10-y*10}deg) contrast(${1.08+y*.12})`},{passive:true});const parts=[];document.querySelectorAll('[data-part]').forEach(b=>b.addEventListener('click',()=>{parts.push(b.dataset.part);b.textContent='Added ✓';b.disabled=true}));document.querySelector('#qform')?.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(e.currentTarget);const text=`Hi Zapify Designs! Autozone quote request.%0AName: ${encodeURIComponent(d.get('name'))}%0APhone: ${encodeURIComponent(d.get('phone'))}%0AService: ${encodeURIComponent(d.get('service'))}%0AMessage: ${encodeURIComponent(d.get('message'))}%0AParts: ${encodeURIComponent(parts.join(', ')||'None')}`;window.open(`https://wa.me/27743899657?text=${text}`,'_blank')});