if(location.pathname.endsWith('/scrapbook.html') && sessionStorage.getItem('zapifyScrapbookUnlocked')!=='1'){window.location.replace('index.html')}
const form=document.querySelector('#unlock');
if(form){form.addEventListener('submit',e=>{e.preventDefault();const pass=document.querySelector('#password');const err=document.querySelector('#error');if(pass.value==='designmemories'){sessionStorage.setItem('zapifyScrapbookUnlocked','1');window.location.href='scrapbook.html'}else{err.textContent='That password does not open this scrapbook. Try the demo password shown below.';pass.focus()}})}
const filters=document.querySelectorAll('[data-filter]');const cards=document.querySelectorAll('.memory[data-type]');filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;cards.forEach(c=>c.hidden=!(f==='all'||c.dataset.type===f))}));
document.querySelectorAll('.chapter-grid button').forEach((b,i)=>b.addEventListener('click',()=>{document.querySelector('#gallery').scrollIntoView({behavior:'smooth'});const target=['photo','photo','place','celebration','note','note'][i];const button=document.querySelector(`[data-filter="${target}"]`);button?.click()}));
