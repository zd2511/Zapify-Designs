let current=0;const pages=[...document.querySelectorAll('.page')],no=document.querySelector('#pageNo');function show(n,dir=1){current=Math.max(0,Math.min(pages.length-1,n));pages.forEach((p,i)=>{p.classList.remove('active','turn-next','turn-prev');if(i===current){p.classList.add('active');p.style.zIndex=20}else{p.style.zIndex=pages.length-i}});no.textContent=`${current+1} / ${pages.length}`;if(dir>0&&current>0)pages[current].classList.add('turn-next');if(dir<0&&current<pages.length-1)pages[current].classList.add('turn-prev')}document.querySelector('#next')?.addEventListener('click',()=>show(current+1,1));document.querySelector('#prev')?.addEventListener('click',()=>show(current-1,-1));document.addEventListener('keydown',e=>{if(e.key==='ArrowRight')show(current+1,1);if(e.key==='ArrowLeft')show(current-1,-1)});show(0);